//  schema is for db
import mongoose from "mongoose";
// from documentation
// const schema = new mongoose.Schema({ name: String, size: String });

// aise kaam nahi karega , schema ka naam dena hoga 
const UserSchema = new mongoose.Schema({ name: String, size: String });